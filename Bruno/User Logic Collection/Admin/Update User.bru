meta {
  name: Update User
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/users/{{userId}}
  body: json
  auth: inherit
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "FirstName": "Jane",
    "LastName": "Doe",
    "Email": "jane@example.com"
  }
}

settings {
  encodeUrl: true
}
