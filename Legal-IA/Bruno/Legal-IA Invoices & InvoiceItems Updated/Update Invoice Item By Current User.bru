meta {
  name: Update Invoice Item By Current User
  type: http
  seq: 6
}

patch {
  url: {{baseUrl}}/invoice-items/user/{{invoiceItemId}}
  body: json
  auth: inherit
}

body:json {
  [
    {
      "ItemId": "{{invoiceItemId}}",
      "UpdateRequest": {
        "invoiceId": "{{invoiceId}}",
        "quantity": 3,
        "total": 1110.00
      }
    }
  ]
}

settings {
  encodeUrl: true
}
