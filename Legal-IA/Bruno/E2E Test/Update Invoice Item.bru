meta {
  name: Update Invoice Item
  type: http
  seq: 5
}

patch {
  url: {{baseUrl}}/invoice-items/user/{{invoiceItemId}}
  body: json
  auth: inherit
}

body:json {
  {
    "id": "{{invoiceItemId}}",
    "invoiceId": "{{invoiceId}}",
    "quantity": 3,
    "total": 1110.00
  }
}

settings {
  encodeUrl: true
}
