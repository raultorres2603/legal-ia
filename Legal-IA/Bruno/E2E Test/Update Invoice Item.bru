meta {
  name: Update Invoice Item
  type: http
  seq: 8
}

patch {
  url: {{baseUrl}}/invoice-items/user
  body: json
  auth: inherit
}

body:json {
  [
    {
      "ItemId": "{{invoiceItemId}}",
      "UpdateRequest": {
        "invoiceId": "{{invoiceId}}",
        "quantity": 3,
        "total": 1110.00
      }
    }
  ]
}

settings {
  encodeUrl: true
}
