meta {
  name: Login User
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/auth/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "Email": "raultorraco@gmail.com",
    "Password": "123"
  }
}

script:post-response {
  bru.setGlobalEnvVar("jwt", res.getBody().token);
}

settings {
  encodeUrl: true
}
