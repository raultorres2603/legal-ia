meta {
  name: Get Form Guidance - Modelo 130 IRPF
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/ai/legal/form-guidance
  body: json
  auth: none
}

body:json {
  {
    "formType": "130",
    "quarter": "Q3",
    "year": 2025,
    "regimeType": "general",
    "activityCode": "7022",
    "userId": "{{userId}}"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain IRPF guidance", function() {
    const responseJson = res.getBody();
    expect(responseJson.formType).to.equal("130");
    expect(responseJson.success).to.be.true;
    expect(responseJson.paymentDeadline).to.be.a('string');
  });
}
