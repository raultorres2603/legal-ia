meta {
  name: Get Form Guidance - Modelo 303 Generic
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/ai/legal/form-guidance
  body: json
  auth: inherit
}

body:json {
  {
    "formType": "303",
    "quarter": "Q3",
    "year": 2025,
    "regimeType": "general",
    "activityCode": "6201"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain form guidance", function() {
    const responseJson = res.getBody();
    expect(responseJson.formType).to.equal("303");
    expect(responseJson.success).to.be.true;
    expect(responseJson.instructions).to.be.a('string');
    expect(responseJson.requiredData).to.be.an('array');
  });
}
