meta {
  name: Classify Legal Question - Legal Topic
  type: http
  seq: 11
}

post {
  url: {{baseUrl}}/ai/legal/classify
  body: json
  auth: inherit
}

body:json {
  {
    "question": "¿Qué es el régimen de estimación objetiva para autónomos?"
  }
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Should classify as legal question", function() {
    const responseJson = res.getBody();
    expect(responseJson.isLegalQuestion).to.be.true;
    expect(responseJson.question).to.be.a('string');
  });
}
