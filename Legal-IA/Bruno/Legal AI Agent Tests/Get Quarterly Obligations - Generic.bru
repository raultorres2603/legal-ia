meta {
  name: Get Quarterly Obligations - Generic
  type: http
  seq: 7
}

get {
  url: {{baseUrl}}/api/ai/legal/quarterly-obligations/2/2025
  body: none
  auth: none
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain quarterly obligations", function() {
    const responseJson = res.getBody();
    expect(responseJson.obligations).to.be.a('string');
    expect(responseJson.obligations).to.contain("trimestre");
    expect(responseJson.obligations).to.contain("2025");
  });
}
