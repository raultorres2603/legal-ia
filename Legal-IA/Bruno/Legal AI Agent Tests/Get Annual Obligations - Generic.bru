meta {
  name: Get Annual Obligations - Generic
  type: http
  seq: 9
}

get {
  url: {{baseUrl}}/ai/legal/annual-obligations/2025
  body: none
  auth: inherit
}

tests {
  test("Status should be 200", function() {
    expect(res.getStatus()).to.equal(200);
  });
  
  test("Response should contain annual obligations", function() {
    const responseJson = res.getBody();
    expect(responseJson.obligations).to.be.a('string');
    expect(responseJson.obligations).to.contain("2025");
    expect(responseJson.obligations).to.contain("anual");
  });
}
