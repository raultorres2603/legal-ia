meta {
  name: Classify Legal Question - Missing Question Field
  type: http
  seq: 16
}

post {
  url: {{baseUrl}}/ai/legal/classify
  body: json
  auth: inherit
}

body:json {
  {
    "NotAQuestion": "This is not the right field"
  }
}

tests {
  test("Status should be 400", function() {
    expect(res.getStatus()).to.equal(400);
  });
  
  test("Should return error for missing question field", function() {
    const responseJson = res.getBody();
    expect(responseJson.error).to.contain("Question field is required");
  });
}
